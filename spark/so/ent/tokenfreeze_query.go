// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/lightsparkdev/spark/so/ent/predicate"
	"github.com/lightsparkdev/spark/so/ent/tokenfreeze"
)

// TokenFreezeQuery is the builder for querying TokenFreeze entities.
type TokenFreezeQuery struct {
	config
	ctx        *QueryContext
	order      []tokenfreeze.OrderOption
	inters     []Interceptor
	predicates []predicate.TokenFreeze
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TokenFreezeQuery builder.
func (tfq *TokenFreezeQuery) Where(ps ...predicate.TokenFreeze) *TokenFreezeQuery {
	tfq.predicates = append(tfq.predicates, ps...)
	return tfq
}

// Limit the number of records to be returned by this query.
func (tfq *TokenFreezeQuery) Limit(limit int) *TokenFreezeQuery {
	tfq.ctx.Limit = &limit
	return tfq
}

// Offset to start from.
func (tfq *TokenFreezeQuery) Offset(offset int) *TokenFreezeQuery {
	tfq.ctx.Offset = &offset
	return tfq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tfq *TokenFreezeQuery) Unique(unique bool) *TokenFreezeQuery {
	tfq.ctx.Unique = &unique
	return tfq
}

// Order specifies how the records should be ordered.
func (tfq *TokenFreezeQuery) Order(o ...tokenfreeze.OrderOption) *TokenFreezeQuery {
	tfq.order = append(tfq.order, o...)
	return tfq
}

// First returns the first TokenFreeze entity from the query.
// Returns a *NotFoundError when no TokenFreeze was found.
func (tfq *TokenFreezeQuery) First(ctx context.Context) (*TokenFreeze, error) {
	nodes, err := tfq.Limit(1).All(setContextOp(ctx, tfq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{tokenfreeze.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tfq *TokenFreezeQuery) FirstX(ctx context.Context) *TokenFreeze {
	node, err := tfq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TokenFreeze ID from the query.
// Returns a *NotFoundError when no TokenFreeze ID was found.
func (tfq *TokenFreezeQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = tfq.Limit(1).IDs(setContextOp(ctx, tfq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{tokenfreeze.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tfq *TokenFreezeQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := tfq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TokenFreeze entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TokenFreeze entity is found.
// Returns a *NotFoundError when no TokenFreeze entities are found.
func (tfq *TokenFreezeQuery) Only(ctx context.Context) (*TokenFreeze, error) {
	nodes, err := tfq.Limit(2).All(setContextOp(ctx, tfq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{tokenfreeze.Label}
	default:
		return nil, &NotSingularError{tokenfreeze.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tfq *TokenFreezeQuery) OnlyX(ctx context.Context) *TokenFreeze {
	node, err := tfq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TokenFreeze ID in the query.
// Returns a *NotSingularError when more than one TokenFreeze ID is found.
// Returns a *NotFoundError when no entities are found.
func (tfq *TokenFreezeQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = tfq.Limit(2).IDs(setContextOp(ctx, tfq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{tokenfreeze.Label}
	default:
		err = &NotSingularError{tokenfreeze.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tfq *TokenFreezeQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := tfq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TokenFreezes.
func (tfq *TokenFreezeQuery) All(ctx context.Context) ([]*TokenFreeze, error) {
	ctx = setContextOp(ctx, tfq.ctx, ent.OpQueryAll)
	if err := tfq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TokenFreeze, *TokenFreezeQuery]()
	return withInterceptors[[]*TokenFreeze](ctx, tfq, qr, tfq.inters)
}

// AllX is like All, but panics if an error occurs.
func (tfq *TokenFreezeQuery) AllX(ctx context.Context) []*TokenFreeze {
	nodes, err := tfq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TokenFreeze IDs.
func (tfq *TokenFreezeQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if tfq.ctx.Unique == nil && tfq.path != nil {
		tfq.Unique(true)
	}
	ctx = setContextOp(ctx, tfq.ctx, ent.OpQueryIDs)
	if err = tfq.Select(tokenfreeze.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tfq *TokenFreezeQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := tfq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tfq *TokenFreezeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, tfq.ctx, ent.OpQueryCount)
	if err := tfq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, tfq, querierCount[*TokenFreezeQuery](), tfq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (tfq *TokenFreezeQuery) CountX(ctx context.Context) int {
	count, err := tfq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tfq *TokenFreezeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, tfq.ctx, ent.OpQueryExist)
	switch _, err := tfq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (tfq *TokenFreezeQuery) ExistX(ctx context.Context) bool {
	exist, err := tfq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TokenFreezeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tfq *TokenFreezeQuery) Clone() *TokenFreezeQuery {
	if tfq == nil {
		return nil
	}
	return &TokenFreezeQuery{
		config:     tfq.config,
		ctx:        tfq.ctx.Clone(),
		order:      append([]tokenfreeze.OrderOption{}, tfq.order...),
		inters:     append([]Interceptor{}, tfq.inters...),
		predicates: append([]predicate.TokenFreeze{}, tfq.predicates...),
		// clone intermediate query.
		sql:  tfq.sql.Clone(),
		path: tfq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TokenFreeze.Query().
//		GroupBy(tokenfreeze.FieldCreateTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (tfq *TokenFreezeQuery) GroupBy(field string, fields ...string) *TokenFreezeGroupBy {
	tfq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TokenFreezeGroupBy{build: tfq}
	grbuild.flds = &tfq.ctx.Fields
	grbuild.label = tokenfreeze.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//	}
//
//	client.TokenFreeze.Query().
//		Select(tokenfreeze.FieldCreateTime).
//		Scan(ctx, &v)
func (tfq *TokenFreezeQuery) Select(fields ...string) *TokenFreezeSelect {
	tfq.ctx.Fields = append(tfq.ctx.Fields, fields...)
	sbuild := &TokenFreezeSelect{TokenFreezeQuery: tfq}
	sbuild.label = tokenfreeze.Label
	sbuild.flds, sbuild.scan = &tfq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TokenFreezeSelect configured with the given aggregations.
func (tfq *TokenFreezeQuery) Aggregate(fns ...AggregateFunc) *TokenFreezeSelect {
	return tfq.Select().Aggregate(fns...)
}

func (tfq *TokenFreezeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range tfq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, tfq); err != nil {
				return err
			}
		}
	}
	for _, f := range tfq.ctx.Fields {
		if !tokenfreeze.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tfq.path != nil {
		prev, err := tfq.path(ctx)
		if err != nil {
			return err
		}
		tfq.sql = prev
	}
	return nil
}

func (tfq *TokenFreezeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TokenFreeze, error) {
	var (
		nodes = []*TokenFreeze{}
		_spec = tfq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TokenFreeze).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TokenFreeze{config: tfq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(tfq.modifiers) > 0 {
		_spec.Modifiers = tfq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tfq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (tfq *TokenFreezeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tfq.querySpec()
	if len(tfq.modifiers) > 0 {
		_spec.Modifiers = tfq.modifiers
	}
	_spec.Node.Columns = tfq.ctx.Fields
	if len(tfq.ctx.Fields) > 0 {
		_spec.Unique = tfq.ctx.Unique != nil && *tfq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, tfq.driver, _spec)
}

func (tfq *TokenFreezeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(tokenfreeze.Table, tokenfreeze.Columns, sqlgraph.NewFieldSpec(tokenfreeze.FieldID, field.TypeUUID))
	_spec.From = tfq.sql
	if unique := tfq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if tfq.path != nil {
		_spec.Unique = true
	}
	if fields := tfq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, tokenfreeze.FieldID)
		for i := range fields {
			if fields[i] != tokenfreeze.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tfq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tfq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tfq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tfq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tfq *TokenFreezeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tfq.driver.Dialect())
	t1 := builder.Table(tokenfreeze.Table)
	columns := tfq.ctx.Fields
	if len(columns) == 0 {
		columns = tokenfreeze.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tfq.sql != nil {
		selector = tfq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tfq.ctx.Unique != nil && *tfq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range tfq.modifiers {
		m(selector)
	}
	for _, p := range tfq.predicates {
		p(selector)
	}
	for _, p := range tfq.order {
		p(selector)
	}
	if offset := tfq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tfq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (tfq *TokenFreezeQuery) ForUpdate(opts ...sql.LockOption) *TokenFreezeQuery {
	if tfq.driver.Dialect() == dialect.Postgres {
		tfq.Unique(false)
	}
	tfq.modifiers = append(tfq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return tfq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (tfq *TokenFreezeQuery) ForShare(opts ...sql.LockOption) *TokenFreezeQuery {
	if tfq.driver.Dialect() == dialect.Postgres {
		tfq.Unique(false)
	}
	tfq.modifiers = append(tfq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return tfq
}

// TokenFreezeGroupBy is the group-by builder for TokenFreeze entities.
type TokenFreezeGroupBy struct {
	selector
	build *TokenFreezeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tfgb *TokenFreezeGroupBy) Aggregate(fns ...AggregateFunc) *TokenFreezeGroupBy {
	tfgb.fns = append(tfgb.fns, fns...)
	return tfgb
}

// Scan applies the selector query and scans the result into the given value.
func (tfgb *TokenFreezeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tfgb.build.ctx, ent.OpQueryGroupBy)
	if err := tfgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TokenFreezeQuery, *TokenFreezeGroupBy](ctx, tfgb.build, tfgb, tfgb.build.inters, v)
}

func (tfgb *TokenFreezeGroupBy) sqlScan(ctx context.Context, root *TokenFreezeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tfgb.fns))
	for _, fn := range tfgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tfgb.flds)+len(tfgb.fns))
		for _, f := range *tfgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tfgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tfgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TokenFreezeSelect is the builder for selecting fields of TokenFreeze entities.
type TokenFreezeSelect struct {
	*TokenFreezeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tfs *TokenFreezeSelect) Aggregate(fns ...AggregateFunc) *TokenFreezeSelect {
	tfs.fns = append(tfs.fns, fns...)
	return tfs
}

// Scan applies the selector query and scans the result into the given value.
func (tfs *TokenFreezeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tfs.ctx, ent.OpQuerySelect)
	if err := tfs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TokenFreezeQuery, *TokenFreezeSelect](ctx, tfs.TokenFreezeQuery, tfs, tfs.inters, v)
}

func (tfs *TokenFreezeSelect) sqlScan(ctx context.Context, root *TokenFreezeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tfs.fns))
	for _, fn := range tfs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tfs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tfs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
