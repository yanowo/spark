// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/lightsparkdev/spark/so/ent/blockheight"
	"github.com/lightsparkdev/spark/so/ent/predicate"
)

// BlockHeightQuery is the builder for querying BlockHeight entities.
type BlockHeightQuery struct {
	config
	ctx        *QueryContext
	order      []blockheight.OrderOption
	inters     []Interceptor
	predicates []predicate.BlockHeight
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BlockHeightQuery builder.
func (bhq *BlockHeightQuery) Where(ps ...predicate.BlockHeight) *BlockHeightQuery {
	bhq.predicates = append(bhq.predicates, ps...)
	return bhq
}

// Limit the number of records to be returned by this query.
func (bhq *BlockHeightQuery) Limit(limit int) *BlockHeightQuery {
	bhq.ctx.Limit = &limit
	return bhq
}

// Offset to start from.
func (bhq *BlockHeightQuery) Offset(offset int) *BlockHeightQuery {
	bhq.ctx.Offset = &offset
	return bhq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (bhq *BlockHeightQuery) Unique(unique bool) *BlockHeightQuery {
	bhq.ctx.Unique = &unique
	return bhq
}

// Order specifies how the records should be ordered.
func (bhq *BlockHeightQuery) Order(o ...blockheight.OrderOption) *BlockHeightQuery {
	bhq.order = append(bhq.order, o...)
	return bhq
}

// First returns the first BlockHeight entity from the query.
// Returns a *NotFoundError when no BlockHeight was found.
func (bhq *BlockHeightQuery) First(ctx context.Context) (*BlockHeight, error) {
	nodes, err := bhq.Limit(1).All(setContextOp(ctx, bhq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{blockheight.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (bhq *BlockHeightQuery) FirstX(ctx context.Context) *BlockHeight {
	node, err := bhq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BlockHeight ID from the query.
// Returns a *NotFoundError when no BlockHeight ID was found.
func (bhq *BlockHeightQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = bhq.Limit(1).IDs(setContextOp(ctx, bhq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{blockheight.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (bhq *BlockHeightQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := bhq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BlockHeight entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BlockHeight entity is found.
// Returns a *NotFoundError when no BlockHeight entities are found.
func (bhq *BlockHeightQuery) Only(ctx context.Context) (*BlockHeight, error) {
	nodes, err := bhq.Limit(2).All(setContextOp(ctx, bhq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{blockheight.Label}
	default:
		return nil, &NotSingularError{blockheight.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (bhq *BlockHeightQuery) OnlyX(ctx context.Context) *BlockHeight {
	node, err := bhq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BlockHeight ID in the query.
// Returns a *NotSingularError when more than one BlockHeight ID is found.
// Returns a *NotFoundError when no entities are found.
func (bhq *BlockHeightQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = bhq.Limit(2).IDs(setContextOp(ctx, bhq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{blockheight.Label}
	default:
		err = &NotSingularError{blockheight.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (bhq *BlockHeightQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := bhq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BlockHeights.
func (bhq *BlockHeightQuery) All(ctx context.Context) ([]*BlockHeight, error) {
	ctx = setContextOp(ctx, bhq.ctx, ent.OpQueryAll)
	if err := bhq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*BlockHeight, *BlockHeightQuery]()
	return withInterceptors[[]*BlockHeight](ctx, bhq, qr, bhq.inters)
}

// AllX is like All, but panics if an error occurs.
func (bhq *BlockHeightQuery) AllX(ctx context.Context) []*BlockHeight {
	nodes, err := bhq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BlockHeight IDs.
func (bhq *BlockHeightQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if bhq.ctx.Unique == nil && bhq.path != nil {
		bhq.Unique(true)
	}
	ctx = setContextOp(ctx, bhq.ctx, ent.OpQueryIDs)
	if err = bhq.Select(blockheight.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (bhq *BlockHeightQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := bhq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (bhq *BlockHeightQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, bhq.ctx, ent.OpQueryCount)
	if err := bhq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, bhq, querierCount[*BlockHeightQuery](), bhq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (bhq *BlockHeightQuery) CountX(ctx context.Context) int {
	count, err := bhq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (bhq *BlockHeightQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, bhq.ctx, ent.OpQueryExist)
	switch _, err := bhq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (bhq *BlockHeightQuery) ExistX(ctx context.Context) bool {
	exist, err := bhq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BlockHeightQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (bhq *BlockHeightQuery) Clone() *BlockHeightQuery {
	if bhq == nil {
		return nil
	}
	return &BlockHeightQuery{
		config:     bhq.config,
		ctx:        bhq.ctx.Clone(),
		order:      append([]blockheight.OrderOption{}, bhq.order...),
		inters:     append([]Interceptor{}, bhq.inters...),
		predicates: append([]predicate.BlockHeight{}, bhq.predicates...),
		// clone intermediate query.
		sql:  bhq.sql.Clone(),
		path: bhq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BlockHeight.Query().
//		GroupBy(blockheight.FieldCreateTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (bhq *BlockHeightQuery) GroupBy(field string, fields ...string) *BlockHeightGroupBy {
	bhq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BlockHeightGroupBy{build: bhq}
	grbuild.flds = &bhq.ctx.Fields
	grbuild.label = blockheight.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//	}
//
//	client.BlockHeight.Query().
//		Select(blockheight.FieldCreateTime).
//		Scan(ctx, &v)
func (bhq *BlockHeightQuery) Select(fields ...string) *BlockHeightSelect {
	bhq.ctx.Fields = append(bhq.ctx.Fields, fields...)
	sbuild := &BlockHeightSelect{BlockHeightQuery: bhq}
	sbuild.label = blockheight.Label
	sbuild.flds, sbuild.scan = &bhq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BlockHeightSelect configured with the given aggregations.
func (bhq *BlockHeightQuery) Aggregate(fns ...AggregateFunc) *BlockHeightSelect {
	return bhq.Select().Aggregate(fns...)
}

func (bhq *BlockHeightQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range bhq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, bhq); err != nil {
				return err
			}
		}
	}
	for _, f := range bhq.ctx.Fields {
		if !blockheight.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if bhq.path != nil {
		prev, err := bhq.path(ctx)
		if err != nil {
			return err
		}
		bhq.sql = prev
	}
	return nil
}

func (bhq *BlockHeightQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BlockHeight, error) {
	var (
		nodes = []*BlockHeight{}
		_spec = bhq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BlockHeight).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BlockHeight{config: bhq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(bhq.modifiers) > 0 {
		_spec.Modifiers = bhq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, bhq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (bhq *BlockHeightQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := bhq.querySpec()
	if len(bhq.modifiers) > 0 {
		_spec.Modifiers = bhq.modifiers
	}
	_spec.Node.Columns = bhq.ctx.Fields
	if len(bhq.ctx.Fields) > 0 {
		_spec.Unique = bhq.ctx.Unique != nil && *bhq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, bhq.driver, _spec)
}

func (bhq *BlockHeightQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(blockheight.Table, blockheight.Columns, sqlgraph.NewFieldSpec(blockheight.FieldID, field.TypeUUID))
	_spec.From = bhq.sql
	if unique := bhq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if bhq.path != nil {
		_spec.Unique = true
	}
	if fields := bhq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, blockheight.FieldID)
		for i := range fields {
			if fields[i] != blockheight.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := bhq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := bhq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := bhq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := bhq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (bhq *BlockHeightQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(bhq.driver.Dialect())
	t1 := builder.Table(blockheight.Table)
	columns := bhq.ctx.Fields
	if len(columns) == 0 {
		columns = blockheight.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if bhq.sql != nil {
		selector = bhq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if bhq.ctx.Unique != nil && *bhq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range bhq.modifiers {
		m(selector)
	}
	for _, p := range bhq.predicates {
		p(selector)
	}
	for _, p := range bhq.order {
		p(selector)
	}
	if offset := bhq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := bhq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (bhq *BlockHeightQuery) ForUpdate(opts ...sql.LockOption) *BlockHeightQuery {
	if bhq.driver.Dialect() == dialect.Postgres {
		bhq.Unique(false)
	}
	bhq.modifiers = append(bhq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return bhq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (bhq *BlockHeightQuery) ForShare(opts ...sql.LockOption) *BlockHeightQuery {
	if bhq.driver.Dialect() == dialect.Postgres {
		bhq.Unique(false)
	}
	bhq.modifiers = append(bhq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return bhq
}

// BlockHeightGroupBy is the group-by builder for BlockHeight entities.
type BlockHeightGroupBy struct {
	selector
	build *BlockHeightQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (bhgb *BlockHeightGroupBy) Aggregate(fns ...AggregateFunc) *BlockHeightGroupBy {
	bhgb.fns = append(bhgb.fns, fns...)
	return bhgb
}

// Scan applies the selector query and scans the result into the given value.
func (bhgb *BlockHeightGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bhgb.build.ctx, ent.OpQueryGroupBy)
	if err := bhgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BlockHeightQuery, *BlockHeightGroupBy](ctx, bhgb.build, bhgb, bhgb.build.inters, v)
}

func (bhgb *BlockHeightGroupBy) sqlScan(ctx context.Context, root *BlockHeightQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(bhgb.fns))
	for _, fn := range bhgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*bhgb.flds)+len(bhgb.fns))
		for _, f := range *bhgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*bhgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bhgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BlockHeightSelect is the builder for selecting fields of BlockHeight entities.
type BlockHeightSelect struct {
	*BlockHeightQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (bhs *BlockHeightSelect) Aggregate(fns ...AggregateFunc) *BlockHeightSelect {
	bhs.fns = append(bhs.fns, fns...)
	return bhs
}

// Scan applies the selector query and scans the result into the given value.
func (bhs *BlockHeightSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bhs.ctx, ent.OpQuerySelect)
	if err := bhs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BlockHeightQuery, *BlockHeightSelect](ctx, bhs.BlockHeightQuery, bhs, bhs.inters, v)
}

func (bhs *BlockHeightSelect) sqlScan(ctx context.Context, root *BlockHeightQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(bhs.fns))
	for _, fn := range bhs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*bhs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bhs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
